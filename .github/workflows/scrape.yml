name: Cloud Outbound Scraper

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Execute Scraper Pipeline
        shell: pwsh
        run: |
          ./run-scraper.ps1 `
            -Query "Residential Roofing Contractors" `
            -BBox @(32.825, -96.754, 32.915, -96.646) `
            -CellSize 2.0 `
            -Depth 3 `
            -Email `
            -OutDir "gmaps-output"

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          git add gmaps-output/
          git commit -m "Automated scrape: updated prospects [skip ci]" || echo "No changes to commit"
          git push
